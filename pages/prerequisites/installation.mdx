import { Tab, Tabs } from 'nextra-theme-docs'
 
# Installation
Choose your package manager, and run the command to install.
<Tabs items={['npm','pnpm', 'yarn', 'bun']}>
  <Tab>
    ```bash copy
    npm install bard-ai
    ```
  </Tab>
  <Tab>
    ```bash copy
    pnpm add bard-ai
    ```
  </Tab>
  <Tab>
    ```bash copy
    yarn add bard-ai
    ```
  </Tab>
  <Tab>
    ```bash copy
    bun add bard-ai
    ```
  </Tab>
</Tabs>

It's as simple as that!
